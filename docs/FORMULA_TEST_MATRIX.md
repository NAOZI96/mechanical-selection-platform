# `winch_drum` 公式测试矩阵

文档版本：0.2.0

适用计算模型：`winch_drum.calc.1.1.0`

本矩阵把 `CALCULATION_SPEC.md` 发布的每个公式 ID 映射到正常、边界及非法输入/不可计算路径。数值期望来自计算规格金样 A-001、直接单位换算或在本文件中可复核的独立算术，不调用生产函数生成期望值。

证据测试集中在 `tests/test_formula_matrix.py::FormulaMatrixTests`。`PASS` 表示自动化行为覆盖完成，不表示参数来源、标准适用性或机械设计已经批准。

| 公式 ID | 正常证据 | 边界证据 | 非法/不可计算证据 | 自动化状态 |
|---|---|---|---|---|
| `UNIT-001` | 100 kN → 100000 N | 1 kN 换算 | 0/负数输入被拒绝 | PASS |
| `UNIT-002` | 20 mm → 0.02 m | 小正数由 Pydantic 保留 | 0/负数输入被拒绝 | PASS |
| `UNIT-003` | 12 m/min → 0.2 m/s | 60 m/min → 1 m/s | 0/负数输入被拒绝 | PASS |
| `UNIT-004` | 400 mm → 0.4 m | 字段可缺失且不产生换算步骤 | 给定 0/负数被拒绝 | PASS |
| `UNIT-005` | 800 mm → 0.8 m | 字段可缺失且不产生换算步骤 | 给定 0/负数被拒绝 | PASS |
| `UNIT-006` | 20 mm → 0.02 m | 0 mm → 0 m | 负数被拒绝 | PASS |
| `FORCE-001` | A-001：120000 N | `K_s=1` | `K_s<1` 被拒绝 | PASS |
| `POWER-001` | A-001：24000 W | 1 kN、60 m/min、`K_s=1` → 1000 W | 依赖的力/速输入非法时阻断 | PASS |
| `POWER-002` | A-001：28235.2941176471 W | `η=1` 时等于负载功率 | `η<=0` 或 `η>1` 被拒绝 | PASS |
| `POWER-003` | 1.0 kW 需求向上选择 1.1 kW | 精确命中档位 | 超过 315 kW 返回超范围而非 315 kW | PASS |
| `DRUM-001` | 给定 0.4 m 芯径原值采用 | 仅证明采用输入，不证明合规 | 给定非正芯径被拒绝 | PASS |
| `DRUM-002` | `R_Dd=20,d=0.02 m` → `D_core=0.38 m` | 结果标为 `preliminary` | `R_Dd<=1` 被拒绝 | PASS |
| `GEOM-001` | `1.05×0.02=0.021 m` | `K_p=1` → 0.02 m | `K_p<1` 被拒绝 | PASS |
| `GEOM-002` | `0.8-2×0.02=0.76 m` | 可用宽度恰好 0.02 m | 不足一节距被拒绝 | PASS |
| `GEOM-003` | `floor(0.76/0.021)=36` | 恰好一整圈时为 1 | 可用宽度不足一圈被拒绝 | PASS |
| `GEOM-004` | `36×0.021=0.756 m` | 一圈占用 0.02 m | 随无效宽度输入阻断 | PASS |
| `CAP-001` | 第一层 `0.4+0.02=0.42 m` | 单层场景 | 非法芯径/层数在计算前被拒绝 | PASS |
| `CAP-002` | 独立螺旋长度 `sqrt((π×0.42)^2+0.021^2)` | 一圈场景 | 非法绳径/节距在计算前被拒绝 | PASS |
| `CAP-003` | 36 圈乘第一层每圈长度 | 一圈毛容量 | 非法面宽/圈数在计算前被拒绝 | PASS |
| `CAP-004` | 两个死圈时按 34 圈可用 | 死圈恰好占满第一层时为 0 | 死圈超过完整圈数被拒绝 | PASS |
| `CAP-005` | A-001 逐层累计金样 | 第一层累计 | 非法几何输入在计算前被拒绝 | PASS |
| `CAP-006` | A-001 实际 6 层 | 目标恰等第一层容量时为 1 层 | 容量不足时为 null 且无成功步骤 | PASS |
| `CAP-007` | A-001 余量 48.9344509321 m | 目标恰等容量时余量为 0 | 容量不足时为 null，并另报缺口 | PASS |
| `CAP-008` | A-001 余量率 16.3114836440% | 零余量时为 0% | 容量不足时为 null | PASS |
| `WIDTH-001` | 32 圈×0.021 m = 0.672 m | 有限整数圈搜索 | 项目初选 D/d 路径仍保持 preliminary | PASS |
| `WIDTH-002` | `0.672+2×0.02=0.712 m` | 两侧余量显式各加一次 | 项目初选 D/d 路径仍保持 preliminary | PASS |
| `SPEED-001` | A-001 空卷工作直径 0.42 m | 单层空/满直径相同 | 非法几何输入在计算前被拒绝 | PASS |
| `SPEED-002` | A-001 满卷工作直径 0.62 m | 单层空/满直径相同 | 容量不足时只写最大层字段，满绳字段为 null | PASS |
| `SPEED-003` | A-001 参考转速 7.3456127581 r/min | 单层参考直径等于空/满直径 | 非法速度/直径在计算前被拒绝 | PASS |
| `SPEED-004` | A-001 空卷 9.0945681767 r/min | 单层与满卷转速相同 | 非法速度/直径在计算前被拒绝 | PASS |
| `SPEED-005` | A-001 满卷 6.1608365068 r/min | 单层与空卷转速相同 | 容量不足时只写最大层字段，满绳字段为 null | PASS |
| `RATIO-001` | A-001 空/满卷 161.6349420272 / 238.6039620401 | 单层空/满速比相同 | 容量不足时只写最大层速比，满绳速比为 null | PASS |
| `RATIO-002` | A-001 名义参考 200.1194520337 | 单层等于空/满速比 | 非法速度/直径在计算前被拒绝 | PASS |
| `BRAKE-001` | A-001 55800 N·m | `K_b=1` → 37200 N·m | `K_b<1` 被拒绝 | PASS |
| `BRAKE-002` | 正向效率近似时 237.0084442967 N·m | 有反向效率时直接计算 | 蜗杆/自锁/禁止反驱不得近似 | PASS |
| `REEVE-001` | 载荷端 `M=4,η=1` → 25000 N 卷筒端力 | 卷筒端输入不换算 | `M<1` 被拒绝 | PASS |
| `REEVE-002` | 载荷端 `M=4` → 0.8 m/s 卷筒端速度 | 卷筒端输入不换算 | `M<1` 被拒绝 | PASS |

## 审核边界

- 自动化通过只能证明当前代码与已发布规格一致。
- `POWER-003`、`DRUM-002`、`BRAKE-001/002` 的可用数值仍取决于已确认的标准、项目规则和制造商数据。
- 机械工程师签字前，所有 `preliminary` 和 `review_required` 项不得用于制造、采购或安全决策。
