# 机械智选实施任务清单

## C-01～C-09 决策状态

- [x] C-01～C-05：输入口径、卷筒几何、死圈、D/d 与系数层级已冻结。
- [x] C-06：静态制动及反向效率公式已冻结；动态/应急制动未实现。
- [x] C-07：稳态电机功率选档已冻结；启动/热容量未实现。
- [x] C-08：显示、同源快照、保留/RPO/RTO/容量策略已形成决策。
- [x] C-09：改为状态化发布门禁，不记录人员或日期。
- [ ] 专项校核：绳强度寿命、绳端固定、卷筒结构、动态/应急制动和热容量、电机启动/惯量/热容量、正式标准条款、制造商数据。

状态说明：`[ ]` 未开始，`[x]` 已完成。当前已完成 Phase 0 设计基线、Phase 1 工程骨架、Phase 2 软件实现、Phase 3 报告/部署候选，以及 Phase 4 目标主机回环部署与恢复门禁；不代表专项工程参数已获批准，也不代表应用已获准公开暴露。

## Phase 0：设计与决策

- [x] 建立 PRD、架构、方案 A 计算规格、API、数据模型、测试和部署基线。
- [x] 定义模块注册契约和方案 B 接入边界。
- [x] 将结果分为 calculated / preliminary / review_required / informational。
- [x] 检查当前输入充分性和潜在重复安全系数。
- [x] 给出逐项验收标准与低资源控制措施。
- [x] 完成跨文档一致性检查；当前模型版本统一为 `winch_drum.calc.1.1.0`，C-01～C-09 字段、状态和报告同源规则已同步。
- [x] 拉力/速度位置改为显式输入并在计算前统一换算到卷筒绳端。
- [x] 卷筒面长冻结为两法兰内侧总面长，两侧余量各扣除一次。
- [x] 固定死圈冻结为严格 2～8 圈、项目初选默认 3 圈并保存来源。
- [x] 未提供批准 D/d 时采用显式项目初选比 20，结果保持 preliminary 并警告。
- [ ] 确认 `service_factor`、`pitch_factor`、`brake_safety_factor` 的项目来源和可用范围。
- [x] 制动基准冻结为设计拉力；反向效率显式输入，默认不近似。
- [x] 电机采用冻结项目功率系列向上选档；工作制只记录、不自动映射系数。
- [x] 冻结显示、PDF 保留、RPO/RTO 与容量策略的软件契约。
- [ ] 由独立机械工程师签署计算规格评审记录。

### Phase 0 出口门禁

- [ ] 所有上述工程决策有负责人、来源和日期；没有未声明数值默认。
- [ ] `CALCULATION_SPEC.md` 公式、输入口径、警告和金样得到审核批准。
- [ ] 产品、机械、软件和质量四方同意 MVP/非范围。

## Phase 1：工程骨架（不含公式扩展）

- [x] 创建 FastAPI/Jinja2/静态资源最小项目及配置分层。
- [x] 实现模块协议、显式注册表、启动契约校验和 `dummy_module` 测试。
- [x] 实现单位规范化、结果等级、警告、公式步骤和版本公共类型。
- [x] 建立 SQLite 迁移、repository、WAL/foreign key/busy timeout 和快照 schema。
- [x] 实现模块列表、schema、统一计算、快照读取和健康端点。
- [x] 配置 Ruff 格式化/静态检查、测试、固定直接依赖版本和 GitHub Actions CI。

### Phase 1A：`winch_drum` 纯计算核心（本次范围）

- [x] 实现 Pydantic 输入模型、严格数值/边界校验和不可变 SI 输入模型。
- [x] 实现显式 `winch_drum` 注册项、模块版本和计算模型版本。
- [x] 实现设计拉力、功率、逐层离散容量、空满卷转速、参考速比和静态制动力矩。
- [x] 实现用户给定卷筒尺寸校核、部分末层、容量不足语义和警告代码。
- [x] 实现用户/批准芯径或显式项目初选 D/d 下的有限候选组合搜索；不声称通用标准合规。
- [x] 完成 25 项金样、单位、边界、单/多层、容量、优化和确定性测试。
- [x] 对照 `CALCULATION_SPEC.md` 完成公式与审计步骤一致性检查。

### Phase 1 出口门禁

- [x] dummy 模块无需更改核心路由/表结构即可端到端运行。
- [x] 数据库在线备份恢复、错误回滚和不重算快照读取测试通过。
- [x] 本地单 worker 进程及 PDF 子进程基线 RSS 有记录；容器实测转入 Phase 4。

## Phase 2：方案 A 计算与界面

- [x] 按公式 ID 实现纯确定性计算器，禁止数据库/网络/时钟副作用。
- [x] 实现 Pydantic 字段与跨字段校验、SI 转换和警告代码。
- [x] 实现逐层离散容绳、反求面长、部分末层和容量不足路径。
- [x] 实现功率、转速/速比范围、静态制动力矩及待复核逻辑。
- [x] 完成中文表单、字段帮助、结果分类、关键汇总和逐层表。
- [x] 保存完整快照；相同模型版本输入可重复。
- [x] 完成金样 A-001、边界、性质、重复系数和安全测试。

### Phase 2 出口门禁

- [x] 37 个公式 ID 均已映射正常/边界/非法或不可计算路径；金样和独立算术由公式测试矩阵追溯。
- [x] 芯径规则、反向效率或电机系列缺失时不生成伪数值。
- [x] 所有高严重度警告在页面显著展示。

## Phase 3：报告与部署候选

- [x] 构建持久化同源报告上下文和 HTML 报告。
- [x] 采用固定版本 ReportLab 和 Noto Sans SC，固定字体哈希、许可与模板版本。
- [x] 实现 PDF 并发 1、超时、临时文件原子写入、哈希和容量保护。
- [x] 完成 PDF 中文、分页、长表、警告、免责声明和 37 页极限样张视觉 QA。
- [x] 编写 Dockerfile/Compose、非 root/只读、资源限制、日志轮转和健康检查。
- [x] 完成本地等效进程性能、内存、并发限流、容量和故障测试；容器/目标机实测转入 Phase 4。

### Phase 3 出口门禁

- [x] HTML、PDF、API、数据库关键值来自同一持久化报告 DTO，不重新计算。
- [x] 本地峰值 RSS 149,626,880 B，PDF 并发严格为 1；512 MiB 容器上限仍需目标机复验。
- [x] 本地无 OOM、无持续增长，容量阈值和成功/失败/超时临时文件清理测试通过。

## Phase 4：受控上线

- [x] 只读盘点腾讯云现有服务、端口、网络、资源与备份空间。
- [x] 冻结本次受控窗口、备份回滚点、资源观察和验收案例。
- [x] 建立独立 Compose 项目/目录与 `127.0.0.1:18080` 回环端口，不改其他服务。
- [x] 执行受控迁移、单容器启动、健康检查、同源 HTML/PDF 与容量不足冒烟。
- [x] 完成 1000 次计算、20 份 PDF 和 5 并发目标容器负载，记录 CPU、RSS、Swap、磁盘及既有服务状态。
- [x] 完成 SQLite 在线备份与隔离可写恢复演练，形成软件验收记录。

### Phase 4 验收边界

- [x] 软件当前仅回环监听，青龙、Mihomo、OpenClaw 未被停止、重建或改配。
- [x] 容器资源/安全限制生效，无 OOM、重启或容器交换区使用。
- [ ] 机械专项校核、正式标准条款和制造商数据关闭后，才可把总发布状态改为 `released`。
- [ ] 如需公网访问，由用户提供域名/代理策略并单独批准；当前不擅自修改现有代理或开放端口。

## 后续：方案 B 接入

- [ ] 单独编制机械传动校核计算规格、输入充分性、标准来源和金样。
- [ ] 实现 `transmission_check` 独立模块协议，不导入方案 A calculator。
- [ ] 如需方案 A→B 数据传递，设计显式版本化 DTO 和用户确认页面。
- [ ] 通过公共契约、快照、报告、资源和工程审核门禁后注册上线。

## 缺陷优先级

- P0：错误工程结果无警告、数据丢失、越权/代码执行、影响同机服务。
- P1：公式/单位/版本错误、重复安全系数、报告与快照不一致、无法恢复。
- P2：非关键功能错误、可理解但不理想的提示/布局。
- P3：文案、样式和低影响改进。
