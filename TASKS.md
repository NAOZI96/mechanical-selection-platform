# 机械智选实施任务清单

状态说明：`[ ]` 未开始，`[x]` 已完成。当前只完成 Phase 0 文档设计，不代表工程参数已批准。

## Phase 0：设计与决策

- [x] 建立 PRD、架构、方案 A 计算规格、API、数据模型、测试和部署基线。
- [x] 定义模块注册契约和方案 B 接入边界。
- [x] 将结果分为 calculated / preliminary / review_required / informational。
- [x] 检查当前输入充分性和潜在重复安全系数。
- [x] 给出逐项验收标准与低资源控制措施。
- [x] 完成跨文档一致性检查：8 个文件齐全；19 个输入字段在计算/API 中一致；模型版本统一为 `winch_drum.calc.1.0.0`；资源上限、状态枚举、公式 ID 与报告同源规则无冲突。
- [ ] 机械负责人确认额定拉力/速度口径均为卷筒绳端。
- [ ] 确认卷筒“有效长度”是两法兰内侧总面长且两侧余量从中扣除。
- [ ] 确认是否必须保留死圈；若是，确定 `dead_wraps` 是否改为必填。
- [ ] 提供可追溯的 D/d 规则（按绳型/结构/工作制）或接受芯径必须手填。
- [ ] 确认 `service_factor`、`pitch_factor`、`brake_safety_factor` 的项目来源和可用范围。
- [ ] 确认制动基准使用额定拉力还是设计拉力，以及反向效率输入方案。
- [ ] 确认电机标准功率系列、工作制字段和是否在 MVP 自动向上选档。
- [ ] 确认显示舍入位数、PDF 保留策略、RPO/RTO 和资源阈值。
- [ ] 由独立机械工程师签署计算规格评审记录。

### Phase 0 出口门禁

- [ ] 所有上述工程决策有负责人、来源和日期；没有未声明数值默认。
- [ ] `CALCULATION_SPEC.md` 公式、输入口径、警告和金样得到审核批准。
- [ ] 产品、机械、软件和质量四方同意 MVP/非范围。

## Phase 1：工程骨架（不含公式扩展）

- [ ] 创建 FastAPI/Jinja2/静态资源最小项目及配置分层。
- [ ] 实现模块协议、显式注册表、启动契约校验和 `dummy_module` 测试。
- [ ] 实现单位规范化、结果等级、警告、公式步骤和版本公共类型。
- [ ] 建立 SQLite 迁移、repository、WAL/foreign key/busy timeout 和快照 schema。
- [ ] 实现模块列表、schema、统一计算、快照读取和健康端点。
- [ ] 配置格式化、静态检查、测试、依赖锁定和 CI（本地或远端方式待定）。

### Phase 1 出口门禁

- [ ] dummy 模块无需更改核心路由/表结构即可端到端运行。
- [ ] 数据库备份恢复、错误回滚和旧快照读取测试通过。
- [ ] 容器内单 worker 基线 RSS 有记录。

## Phase 2：方案 A 计算与界面

- [ ] 按公式 ID 实现纯确定性计算器，禁止数据库/网络/时钟副作用。
- [ ] 实现 Pydantic 字段与跨字段校验、SI 转换和警告代码。
- [ ] 实现逐层离散容绳、反求面长、部分末层和容量不足路径。
- [ ] 实现功率、转速/速比范围、静态制动力矩及待复核逻辑。
- [ ] 完成中文表单、字段帮助、结果分类、关键汇总和逐层表。
- [ ] 保存完整快照；相同模型版本输入可重复。
- [ ] 完成金样 A-001、边界、性质、重复系数和安全测试。

### Phase 2 出口门禁

- [ ] 每个公式 ID 至少有正常/边界/异常测试，金样与独立手算一致。
- [ ] 芯径规则、反向效率或电机系列缺失时不生成伪数值。
- [ ] 所有高严重度警告在页面显著展示。

## Phase 3：报告与部署候选

- [ ] 构建同源报告上下文和 HTML 报告。
- [ ] 选择并验证低内存中文 PDF 引擎，固定字体与模板版本。
- [ ] 实现 PDF 并发 1、超时、临时文件原子写入、哈希和容量保护。
- [ ] 完成 PDF 中文、分页、长表、警告、免责声明和视觉 QA。
- [ ] 编写 Dockerfile/Compose、非 root/只读、资源限制、日志轮转和健康检查。
- [ ] 在等效 2 核/1.9 GB 环境完成性能、内存、磁盘和故障测试。

### Phase 3 出口门禁

- [ ] HTML、PDF、API、数据库关键值一致且不重新计算。
- [ ] Web 容器在初始 512 MiB 上限内稳定，PDF 并发严格为 1。
- [ ] 无 OOM/持续 Swap；磁盘阈值与临时文件清理有效。

## Phase 4：受控上线

- [ ] 只读盘点腾讯云现有服务、端口、网络、资源与备份空间。
- [ ] 明确部署窗口、回滚负责人、监控窗口和验收案例。
- [ ] 建立独立 Compose 项目、目录、秘密和回环端口，不改其他服务。
- [ ] 执行备份、迁移预检、单容器启动、健康检查和受控 PDF 冒烟。
- [ ] 小流量观察主机/既有服务，记录 CPU、RSS、Swap、I/O 和磁盘。
- [ ] 完成恢复演练和上线签字。

## 后续：方案 B 接入

- [ ] 单独编制机械传动校核计算规格、输入充分性、标准来源和金样。
- [ ] 实现 `transmission_check` 独立模块协议，不导入方案 A calculator。
- [ ] 如需方案 A→B 数据传递，设计显式版本化 DTO 和用户确认页面。
- [ ] 通过公共契约、快照、报告、资源和工程审核门禁后注册上线。

## 缺陷优先级

- P0：错误工程结果无警告、数据丢失、越权/代码执行、影响同机服务。
- P1：公式/单位/版本错误、重复安全系数、报告与快照不一致、无法恢复。
- P2：非关键功能错误、可理解但不理想的提示/布局。
- P3：文案、样式和低影响改进。
