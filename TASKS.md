# 机械智选实施任务清单

## C-01～C-09 决策状态

- [x] C-01～C-05：输入口径、卷筒几何、死圈、D/d 与系数层级已冻结。
- [x] C-06：静态制动及反向效率公式已冻结；动态/应急制动未实现。
- [x] C-07：稳态电机功率选档已冻结；启动/热容量未实现。
- [x] C-08：显示、同源快照、保留/RPO/RTO/容量策略已形成决策。
- [x] C-09：改为状态化发布门禁，不记录人员或日期。
- [ ] 专项校核：绳强度寿命、绳端固定、卷筒结构、动态/应急制动和热容量、电机启动/惯量/热容量、正式标准条款、制造商数据。

状态说明：`[ ]` 未开始，`[x]` 已完成。当前已完成 Phase 0～6 的首发模块软件与既有目标主机回环验收、Phase 7 八个扩展模块的本地软件实现，以及 Phase 8 九模块候选版的产品化界面与安全/数据兼容收尾；不代表专项工程参数已获批准，也不代表当前九模块候选版已远程部署或获准公开暴露。

## Phase 0：设计与决策

- [x] 建立 PRD、架构、方案 A 计算规格、API、数据模型、测试和部署基线。
- [x] 定义模块注册契约和方案 B 接入边界。
- [x] 将结果分为 calculated / preliminary / review_required / informational。
- [x] 检查当前输入充分性和潜在重复安全系数。
- [x] 给出逐项验收标准与低资源控制措施。
- [x] 完成跨文档一致性检查；当前模型版本统一为 `winch_drum.calc.1.2.0`，C-01～C-09 字段、状态和报告同源规则已同步。
- [x] 拉力/速度位置改为显式输入并在计算前统一换算到卷筒绳端。
- [x] 卷筒面长冻结为两法兰内侧总面长，两侧余量各扣除一次。
- [x] 固定死圈冻结为严格 2～8 圈、项目初选默认 3 圈并保存来源。
- [x] 未提供批准 D/d 时采用显式项目初选比 20，结果保持 preliminary 并警告。
- [x] 实际 D/d 低于当前最小值时生成含实际值和建议芯径的高风险警告，页面禁止据此输出整机“设计合格”。
- [ ] 确认 `service_factor`、`pitch_factor`、`brake_safety_factor` 的项目来源和可用范围。
- [x] 制动基准冻结为设计拉力；反向效率显式输入，默认不近似。
- [x] 电机采用冻结项目功率系列向上选档；工作制只记录、不自动映射系数。
- [x] 冻结显示、PDF 保留、RPO/RTO 与容量策略的软件契约。
- [ ] 由独立机械工程师签署计算规格评审记录。

### Phase 0 出口门禁

- [ ] 所有上述工程决策有负责人、来源和日期；没有未声明数值默认。
- [ ] `CALCULATION_SPEC.md` 公式、输入口径、警告和金样得到审核批准。
- [ ] 产品、机械、软件和质量四方同意 MVP/非范围。

## Phase 1：工程骨架（不含公式扩展）

- [x] 创建 FastAPI/Jinja2/静态资源最小项目及配置分层。
- [x] 实现模块协议、显式注册表、启动契约校验和 `dummy_module` 测试。
- [x] 实现单位规范化、结果等级、警告、公式步骤和版本公共类型。
- [x] 建立 SQLite 迁移、repository、WAL/foreign key/busy timeout 和快照 schema。
- [x] 实现模块列表、schema、统一计算、快照读取和健康端点。
- [x] 配置 Ruff 格式化/静态检查、测试、固定直接依赖版本和 GitHub Actions CI。

### Phase 1A：`winch_drum` 纯计算核心（本次范围）

- [x] 实现 Pydantic 输入模型、严格数值/边界校验和不可变 SI 输入模型。
- [x] 实现显式 `winch_drum` 注册项、模块版本和计算模型版本。
- [x] 实现设计拉力、功率、逐层离散容量、空满卷转速、参考速比和静态制动力矩。
- [x] 实现用户给定卷筒尺寸校核、部分末层、容量不足语义和警告代码。
- [x] 实现用户/批准芯径或显式项目初选 D/d 下的有限候选组合搜索；不声称通用标准合规。
- [x] 完成 25 项金样、单位、边界、单/多层、容量、优化和确定性测试。
- [x] 对照 `CALCULATION_SPEC.md` 完成公式与审计步骤一致性检查。

### Phase 1 出口门禁

- [x] dummy 模块无需更改核心路由/表结构即可端到端运行。
- [x] 数据库在线备份恢复、错误回滚和不重算快照读取测试通过。
- [x] 本地单 worker 进程及 PDF 子进程基线 RSS 有记录；容器实测转入 Phase 4。

## Phase 2：方案 A 计算与界面

- [x] 按公式 ID 实现纯确定性计算器，禁止数据库/网络/时钟副作用。
- [x] 实现 Pydantic 字段与跨字段校验、SI 转换和警告代码。
- [x] 实现逐层离散容绳、反求面长、部分末层和容量不足路径。
- [x] 实现功率、转速/速比范围、静态制动力矩及待复核逻辑。
- [x] 完成中文表单、字段帮助、结果分类、关键汇总和逐层表。
- [x] 在当前浏览器标签页访问期间自动保存并恢复表单参数与最近计算结果，报告往返不丢失选型现场。
- [x] 在参数区提供“清空参数”操作，经确认后恢复初始表单并清除当前标签页中的最近计算结果。
- [x] 保存完整快照；相同模型版本输入可重复。
- [x] 完成金样 A-001、边界、性质、重复系数和安全测试。

### Phase 2 出口门禁

- [x] 37 个公式 ID 均已映射正常/边界/非法或不可计算路径；金样和独立算术由公式测试矩阵追溯。
- [x] 芯径规则、反向效率或电机系列缺失时不生成伪数值。
- [x] 所有高严重度警告在页面显著展示。

## Phase 3：报告与部署候选

- [x] 构建持久化同源报告上下文和 HTML 报告。
- [x] 采用固定版本 ReportLab 和 Noto Sans SC，固定字体哈希、许可与模板版本。
- [x] 报告增加返回/PDF 操作入口，输入字段、等级和来源中文展示，公式按表达式、代入值和结果分层排版。
- [x] 绳索、载荷与环境记录提供中文默认值和可自定义中文备选词库，不将记录字段误作工程枚举。
- [x] 实现 PDF 并发 1、超时、临时文件原子写入、哈希和容量保护。
- [x] 完成 PDF 中文、分页、长表、警告、免责声明和 37 页极限样张视觉 QA。
- [x] 编写 Dockerfile/Compose、非 root/只读、资源限制、日志轮转和健康检查。
- [x] 完成本地等效进程性能、内存、并发限流、容量和故障测试；容器/目标机实测转入 Phase 4。

### Phase 3 出口门禁

- [x] HTML、PDF、API、数据库关键值来自同一持久化报告 DTO，不重新计算。
- [x] 本地峰值 RSS 149,626,880 B，PDF 并发严格为 1；512 MiB 容器上限仍需目标机复验。
- [x] 本地无 OOM、无持续增长，容量阈值和成功/失败/超时临时文件清理测试通过。

## Phase 4：受控上线

- [x] 只读盘点腾讯云现有服务、端口、网络、资源与备份空间。
- [x] 冻结本次受控窗口、备份回滚点、资源观察和验收案例。
- [x] 建立独立 Compose 项目/目录与 `127.0.0.1:18080` 回环端口，不改其他服务。
- [x] 执行受控迁移、单容器启动、健康检查、同源 HTML/PDF 与容量不足冒烟。
- [x] 完成 1000 次计算、20 份 PDF 和 5 并发目标容器负载，记录 CPU、RSS、Swap、磁盘及既有服务状态。
- [x] 完成 SQLite 在线备份与隔离可写恢复演练，形成软件验收记录。

### Phase 4 验收边界

- [x] 软件当前仅回环监听，青龙、Mihomo、OpenClaw 未被停止、重建或改配。
- [x] 容器资源/安全限制生效，无 OOM、重启或容器交换区使用。
- [ ] 机械专项校核、正式标准条款和制造商数据关闭后，才可把总发布状态改为 `released`。
- [x] 用户已批准公网问题处理；Caddy 只反代回环应用，TLS、连接/响应超时、友好 503、HEAD 与爬虫基础文件已完成。
- [ ] 由域名主体在腾讯云完成或确认 ICP 备案/接入，再验证国内外 HTTPS、搜索引擎抓取和可用性监控。

## Phase 5：平台主页与模块目录

- [x] 将 `/` 从单一计算器调整为机械智选平台主页，保留 `/modules/winch_drum` 稳定入口。
- [x] 以运行时注册表生成已开放模块，增加统一 `/modules/{module_id}` 页面路由。
- [x] 建立只读规划模块目录，明确标记“规划中”且不生成伪入口或计算 API。
- [x] 注册模块自动替换同 ID 规划卡片，新增模块无需修改主页模板。
- [x] 精简首屏操作与工程图信息，建立完整模块目录和统一“全部功能模块”入口。
- [x] 将主页下半区重构为面向访客的功能原理与平台理念说明。
- [x] 统一首页、计算工作台、报告与错误页的本地字体栈、字号层级、行高和重点信息视觉权重。
- [x] 完成桌面与 390 px 移动视口视觉检查、主页/API/模块目录回归和文档同步。

### Phase 5 出口门禁

- [x] 已开放与规划状态不会混淆，主页只把已注册且配置页面模板的模块作为可进入工具。
- [x] 现有绞车计算、快照、HTML/PDF 报告和计算模型版本保持不变。
- [x] 未引入新框架、数据库、常驻服务或工程公式。

## Phase 6：计算页状态与交互升级

- [x] 将右侧结果区明确拆分为“等待计算 / 正在计算 / 计算结果”三个互斥状态，修复计算成功后等待页与结果并存的问题。
- [x] 增加结果区忙碌状态、无障碍状态通知、返回参数入口，并缩短空白等待区在桌面和移动端的无效占高。
- [x] 增加页面状态契约回归测试，完成单元、输入边界、公式回归、API、PDF、静态检查和浏览器交互验证。
- [x] Phase 6 仅调整展示与交互状态，当时未修改工程公式、SI 输入口径、快照结构、计算模型版本或报告模型版本；后续 Phase 8 的发布状态快照治理单独版本化。

### Phase 6 后续门禁

- [ ] 如需历史快照列表、项目预设或参数导入导出，先确认访问控制、留存规则和产品需求，再单独立项；当前不创建未经批准的入口。

## Phase 7：八个受控工程工作表

- [x] 完成独立需求分析并冻结八模块范围、非范围、输入来源与发布状态，见 [`docs/MODULE_REQUIREMENTS.md`](docs/MODULE_REQUIREMENTS.md)。
- [x] 实现并注册 `transmission_check`、`gear_drive`、`shaft_bearing`、`lead_screw`、`synchronous_belt`、`motor_drive`、`stepper_motor` 和 `pneumatic_cylinder`。
- [x] 八模块均使用独立 Pydantic schema 和确定性计算器，不导入 `winch_drum` 业务计算。
- [x] 八模块接入统一模块发现、schema、计算、快照、HTML 报告和 PDF 报告路径。
- [x] 增加通用受控工作台、中文字段/结果标签、显式非推荐验证算例和逐模块工程发布状态。
- [x] 软件可运行状态与工程发布状态分离：`winch_drum=engineering_review`，八个扩展模块均为 `internal_testing`。
- [x] 形成八模块计算规格和公式追溯入口：[`docs/EXPANDED_MODULES_CALCULATION_SPEC.md`](docs/EXPANDED_MODULES_CALCULATION_SPEC.md)、[`docs/EXPANDED_FORMULA_TEST_MATRIX.md`](docs/EXPANDED_FORMULA_TEST_MATRIX.md)。
- [x] 当前九模块候选版 126 项本地回归全部通过，覆盖单元、边界、派生数值安全、公式库存防漂移、产品页面、API、HTML/PDF、受控 500 错误页、schema v4、遗留数据和持久化契约。
- [x] 八模块继续复用通用 JSON 快照和报告表，没有增加模块专属列或常驻服务，也未执行远程部署；计算时发布状态持久化由后续 Phase 8 的通用迁移 `005` 完成。
- [ ] 如需模块间数据传递，另行设计显式版本化 DTO 和用户确认页面；当前模块之间不隐式读取彼此结果。

### Phase 7 工程发布门禁

- [ ] `transmission_check`：确认每级速比/效率来源、额定与峰值边界、反驱和热容量适用性，并完成独立机械复算。
- [ ] `gear_drive`：确认齿形、精度、材料、润滑、许用应力与正式标准条款；完成齿根/接触强度及制造商校核。
- [ ] `shaft_bearing`：确认轴承 X/Y/p 与额定数据、载荷谱/可靠度/润滑；完成轴肩、键槽、疲劳、挠度和临界转速校核。
- [ ] `lead_screw`：确认螺纹型式、摩擦系数、支承系数和材料；完成螺纹强度、磨损、临界转速与供应商校核。
- [ ] `synchronous_belt`：确认制造商手册版本、带型/带宽、啮合修正、张紧、寿命和轴载荷。
- [ ] `motor_drive`：补充加减速惯量、启动/峰值/热容量、工作制、供电与制造商转矩速度曲线。
- [ ] `stepper_motor`：确认负载惯性加速过程的传动损耗口径、完整转矩速度曲线、驱动器/电压、共振、失步裕量、热和定位精度。
- [ ] `pneumatic_cylinder`：确认压力损失、摩擦、缓冲、速度、安装/侧载/屈曲、阀与管路、制造商额定值。
- [ ] 每个模块的独立审核金样、标准/制造商版本、参数来源和适用范围均有关闭证据后，才可把对应状态提升为 `engineering_review` 或 `released`。
- [ ] 九模块版本在目标主机重新完成资源、备份恢复、PDF 并发和既有服务影响复验后，才可更新远程部署验收记录。

## Phase 8：产品化与发布状态数据治理

- [x] 将主页收敛为可直接试用的九模块产品入口，提供名称/说明/能力搜索、分类筛选、结果计数和无匹配提示；无需第三方动画运行库或前端 CDN。
- [x] 将 `/docs` 与 `/redoc` 改为服务端渲染的 CSP-safe API 参考，端点列表从当前 OpenAPI schema 派生，不执行内联脚本。
- [x] 统一 CSP、`X-Content-Type-Options`、`Referrer-Policy`、`X-Frame-Options` 和 `Permissions-Policy`；HTTPS 公共根地址下启用 HSTS。
- [x] 冻结缓存策略：`/static/*` 为 `public, max-age=86400`，计算/报告路径为 `no-store` 且禁止索引，其余页面为 `no-cache`。
- [x] 新增可空迁移 `005_calculation_release_status.sql`，将计算时的 `release_status` 持久化；旧行空值读取为 `legacy_unknown`，不按当前注册表状态回填。
- [x] 新计算升级为 snapshot schema v4 / report context schema v4；报告上下文保存发布状态及中文标签，工程公式、SI 输入口径和全部 `calculation_model_version` 保持不变。
- [x] 报告模板升级为 `winch_drum.report.1.2.1` 与八模块 `*.report.1.0.1`，使新增发布状态展示与旧模板缓存明确隔离。
- [x] 冻结旧报告策略：通过相对路径、大小与 SHA-256 校验的遗留缓存 PDF 可继续下载并带 `legacy_unknown` 告警；无有效缓存的旧快照返回 `409 LEGACY_RELEASE_STATUS_MISSING`，不得用当前发布状态重建。
- [x] 启动时检查固定 Noto Sans SC 字体及报告/临时目录可写；`/health/ready` 只检查注册表、已应用迁移和报告运行目录/字体存在，不执行完整数据库完整性检查或 PDF 试渲染。
- [ ] 在目标主机在线备份后受控应用迁移 `005`，再完成九模块计算、HTML/PDF、旧缓存、资源、恢复和既有服务影响复验。

### Phase 8 发布门禁

- [x] 当前候选版 126 项全量单元、边界、公式回归、API、PDF、迁移、兼容、受控错误页与安全头测试通过。
- [ ] 生产 `DESIGN_AGENT_AUTO_MIGRATE=false`；不得在未备份的目标库上通过临时开启自动迁移绕过门禁。
- [ ] `legacy_unknown` 只表示旧记录缺少当时发布状态，必须按内部测试边界展示，不得推断为当时已评审或已放行。
- [ ] Phase 4 历史验收只证明当时 `001`～`004` 的首发模块镜像；迁移 `005` 和当前候选版仍需独立目标机证据。

## 缺陷优先级

- P0：错误工程结果无警告、数据丢失、越权/代码执行、影响同机服务。
- P1：公式/单位/版本错误、重复安全系数、报告与快照不一致、无法恢复。
- P2：非关键功能错误、可理解但不理想的提示/布局。
- P3：文案、样式和低影响改进。
