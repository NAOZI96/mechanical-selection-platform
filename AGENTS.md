# AGENTS.md

本文件适用于项目根目录及其全部子目录。执行任务时，必须同时遵守用户当前指令；若规则存在冲突，以更高优先级指令和更具体目录中的 `AGENTS.md` 为准。

# 项目目标

本项目是可扩展的机械设备计算与选型平台。  
当前仅开发 `winch_drum` 绞车与卷筒选型模块。

# 工程计算规则

- 所有计算使用确定性 Python 代码。
- 内部统一采用 SI 单位。
- 页面单位转换不能改变原始计算逻辑。
- 禁止虚构标准、经验系数和产品参数。
- 默认值必须注明来源状态：项目设定、工程经验或待确认。
- 计算结果必须区分：理论值、初选值、建议值、待校核值。
- 修改公式时必须同步更新 `docs/CALCULATION_SPEC.md` 和测试。
- 不得仅通过修改测试期望值来使测试通过；必须先核对公式、单位、输入口径和独立计算依据。
- 容绳量使用逐层离散计算。
- 安全系数和制动系数不得无说明重复叠加。
- 所有报告必须带计算模型版本和免责声明。

# 软件架构规则

- 技术栈采用 FastAPI、Jinja2、原生 JavaScript、普通 CSS 和 SQLite。
- 禁止未经批准引入 React、Vue、Redis、Celery 或 PostgreSQL。
- 所有选型工具必须通过模块注册表接入。
- 业务计算必须与 Web 路由、数据库访问和 PDF 生成解耦。
- `calculator.py` 不得依赖 FastAPI、数据库、PDF 渲染器或 Web 上下文。
- 一个函数只完成一个明确的计算职责。
- 公共接口必须使用类型标注。
- 所有用户输入必须通过 Pydantic 验证；前端校验不能替代后端校验。
- 不允许在源代码中写入密码、Token、域名或服务器 IP。

# 服务器约束

- 目标服务器只有 2 核 CPU 和 1.9 GB 物理内存。
- 应用只运行单个 Web worker。
- 禁止启动不必要的常驻服务。
- 不得修改、停止或重建现有 Docker、青龙、OpenClaw 和 Mihomo 服务。
- 部署前必须检查端口冲突、可用内存、CPU、Swap、磁盘空间和现有服务状态。
- 应用初期只监听 `127.0.0.1`，不直接暴露管理端口。

# 测试要求

每次完成功能后必须运行：

- 单元测试；
- 输入边界测试；
- 公式回归测试；
- API 测试；
- PDF 生成测试。

任何公式修改都必须增加对应测试案例。测试期望值应来自独立手算、经审核的金样或其他可追溯依据。

# 文档要求

代码变更后必须检查是否需要同步更新：

- `README.md`
- `docs/CALCULATION_SPEC.md`
- `docs/API_SPEC.md`
- `docs/DEPLOYMENT.md`
- `TASKS.md`

若判定无需更新，应在完成汇报中说明。

# 工作方式

- 开始任务前先阅读相关文档和现有代码。
- 先给出简短计划，再修改代码。
- 不扩大任务范围。
- 不顺手重构无关模块。
- 保留用户已有且与当前任务无关的改动。
- 完成后汇报修改文件、测试结果、风险和待确认项。
